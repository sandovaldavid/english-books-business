---
import SocialNetworkIcon from '../icons/SocialNetwork.astro';
import LocationIcon from '../icons/Location.astro';

const { class: className = '' } = Astro.props;
---

<div class={`schedule-banner ${className}`}>
	<div
		class='bg-gradient-to-r from-primary/10 to-primary-light/5 border border-primary-light/20 rounded-xl lg:shadow-md lg:hover:shadow-lg lg:transition-all lg:duration-300 p-5'>
		<div class='flex flex-col md:flex-row items-center gap-4'>
			<div class='flex-1 flex flex-col space-y-3'>
				<div class='flex items-center mb-4'>
					<span class='bg-primary text-white p-2 rounded-full mr-3'>
						<LocationIcon class='w-5 h-5' />
					</span>
					<h3
						class='font-bold text-lg md:text-xl lg:text-2xl text-primary-dark'>
						Ubicación virtual
					</h3>
				</div>
			</div>
		</div>
		<p class='text-gray-600 mb-4'>
			FluentReads es una tienda completamente digital. Ofrecemos todos
			nuestros materiales en formato electrónico con entrega inmediata.
		</p>
	</div>
</div>

<style>
	.schedule-banner {
		animation: fadeIn 0.6s ease-in-out;
	}

	@keyframes fadeIn {
		from {
			opacity: 0;
			transform: translateY(10px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	/* Pulse animation for the clock icon */
	@media (min-width: 768px) {
		.schedule-banner:hover :global(svg) {
			animation: pulse 1.5s infinite;
		}
	}

	@keyframes pulse {
		0% {
			transform: scale(1);
		}
		50% {
			transform: scale(1.05);
		}
		100% {
			transform: scale(1);
		}
	}
</style>
